---
// src/pages/services/index.astro
import Layout from '../../layouts/Layout.astro';
---

<Layout title="Услуги - StandartSoft Plus">
  <section class="hero-section">
    <div class="container">
      <h1>Наши услуги</h1>
      <p class="slogan">Профессиональная разработка мобильных приложений и веб-сервисов</p>
    </div>
  </section>

  <section class="services-grid">
    <div class="container">
      <div class="service-card">
        <h3>Мобильные приложения</h3>
        <p>Разработка нативных и кроссплатформенных мобильных приложений для iOS и Android</p>
      </div>

      <div class="service-card">
        <h3>Веб-сервисы</h3>
        <p>Создание современных веб-приложений и сервисов с использованием актуальных технологий</p>
      </div>

      <div class="service-card">
        <h3>Консалтинг</h3>
        <p>Экспертная оценка проектов, аудит кода и консультации по архитектуре</p>
      </div>
    </div>
  </section>
</Layout>

<style>
  .hero-section {
    padding: 4rem 2rem;
    text-align: center;
    background: linear-gradient(135deg, var(--color-primary), var(--color-accent-1));
    color: white;
  }

  h1 {
    font-family: var(--font-heading);
    font-weight: 200;
    font-size: 3rem;
    margin-bottom: 1rem;
  }

  .slogan {
    font-family: var(--font-body);
    font-size: 1.125rem;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    opacity: 0.95;
  }

  .services-grid {
    padding: 4rem 2rem;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
  }

  .service-card {
    background: white;
    padding: 2rem;
    border-radius: 12px;
    box-shadow: 0 4px 20px rgba(61, 17, 57, 0.08);
    border: 1px solid rgba(61, 17, 57, 0.05);
    transition: all 0.3s ease;
  }

  .service-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 30px rgba(61, 17, 57, 0.12);
    border-color: var(--color-secondary);
  }

  .service-card h3 {
    font-family: var(--font-heading);
    font-weight: 300;
    color: var(--color-primary);
    margin-bottom: 1rem;
  }

  .service-card p {
    color: var(--color-text);
    opacity: 0.8;
    line-height: 1.6;
  }

  @media (max-width: 768px) {
    h1 {
      font-size: 2rem;
    }

    .container {
      grid-template-columns: 1fr;
    }
  }
</style>