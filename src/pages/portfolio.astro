---
import { getCollection } from 'astro:content';
import type { CollectionEntry } from 'astro:content';
import Layout from '../layouts/Layout.astro';
import CTABlock from '../components/CTABlock.astro';

const base = import.meta.env.BASE_URL;

// Получаем проекты из Content Collection
const allJobs = await getCollection('jobs');
const projects = allJobs
  .sort((a: CollectionEntry<'jobs'>, b: CollectionEntry<'jobs'>) => a.data.order - b.data.order)
  .map((job: CollectionEntry<'jobs'>) => ({
    id: job.slug,
    ...job.data
  }));
---

<Layout title="Портфолио - StandartSoft Plus">
  <!-- Hero секция -->
  <section class="hero-section">
    <div class="hero-container">
      <div class="hero-content">
        <h1>Наши проекты</h1>
        <p class="slogan">Опыт разработки комплексных решений для различных отраслей бизнеса. От идеи до запуска в эксплуатацию.</p>
      </div>
      <div class="hero-decoration">
        <div class="decoration-circle">
          <svg viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
            <path d="M22 19a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h5l2 3h9a2 2 0 0 1 2 2z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </div>
      </div>
    </div>
  </section>

  <!-- Список проектов -->
  <section class="projects-section">
    <div class="container">
      <div class="projects-grid">
        {projects.map((project: any) => (
          <article class="project-card">
            <div class="project-card-header">
              <h2>{project.title}</h2>
              <p class="project-description">{project.description}</p>
            </div>

            <div class="project-card-body">
              <p class="project-details">{project.details}</p>

              <div class="project-features">
                <h4>Основные возможности:</h4>
                <ul>
                  {project.features.map((feature: string) => (
                    <li>{feature}</li>
                  ))}
                </ul>
              </div>

              <div class="project-meta">
                <div class="meta-item">
                  <span class="meta-label">Клиент:</span>
                  <span class="meta-value">{project.client}</span>
                </div>
                <div class="meta-item">
                  <span class="meta-label">Стек:</span>
                  <span class="meta-value">{project.stack}</span>
                </div>
                <div class="meta-item">
                  <span class="meta-label">Статус:</span>
                  <span class="meta-value status-badge">{project.status}</span>
                </div>
              </div>

              <div class="project-tags">
                {project.tags.map((tag: string) => (
                  <span class="tag">{tag}</span>
                ))}
              </div>
            </div>

            <div class="project-card-footer">
              <a href={`${base}jobs/${project.id}/`} class="btn btn-primary">
                Подробнее о проекте →
              </a>
            </div>
          </article>
        ))}
      </div>
    </div>
  </section>

  <!-- Статистика -->
  <section class="stats-section">
    <div class="container">
      <h2 class="text-center">В цифрах</h2>
      
      <div class="stats-grid">
        <div class="stat-card">
          <div class="stat-number">2+</div>
          <div class="stat-label">Года на рынке</div>
        </div>
        
        <div class="stat-card">
          <div class="stat-number">10+</div>
          <div class="stat-label">Реализованных проектов</div>
        </div>
        
        <div class="stat-card">
          <div class="stat-number">100%</div>
          <div class="stat-label">Сданных в срок</div>
        </div>
        
        <div class="stat-card">
          <div class="stat-number">24/7</div>
          <div class="stat-label">Поддержка проектов</div>
        </div>
      </div>
    </div>
  </section>

  <!-- Процесс работы -->
  <section class="process-section">
    <div class="container-narrow">
      <h2 class="text-center">Как мы работаем</h2>
      
      <div class="process-steps">
        <div class="process-step">
          <div class="step-number">01</div>
          <div class="step-content">
            <h3>Анализ задачи</h3>
            <p>Изучаем бизнес-процессы, формируем требования, определяем технологический стек</p>
          </div>
        </div>

        <div class="process-step">
          <div class="step-number">02</div>
          <div class="step-content">
            <h3>Проектирование</h3>
            <p>Разрабатываем архитектуру, создаём прототипы интерфейсов, согласовываем решения</p>
          </div>
        </div>

        <div class="process-step">
          <div class="step-number">03</div>
          <div class="step-content">
            <h3>Разработка</h3>
            <p>Итеративная разработка с регулярными демонстрациями прогресса и сбором обратной связи</p>
          </div>
        </div>

        <div class="process-step">
          <div class="step-number">04</div>
          <div class="step-content">
            <h3>Тестирование</h3>
            <p>Комплексное тестирование функциональности, производительности и безопасности</p>
          </div>
        </div>

        <div class="process-step">
          <div class="step-number">05</div>
          <div class="step-content">
            <h3>Запуск</h3>
            <p>Развёртывание в production, обучение пользователей, передача документации</p>
          </div>
        </div>

        <div class="process-step">
          <div class="step-number">06</div>
          <div class="step-content">
            <h3>Поддержка</h3>
            <p>Техническая поддержка, обновления, доработки и развитие функциональности</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA -->
  <CTABlock 
    title="Обсудим ваш проект?"
    description="Обсудим ваш проект и предложим оптимальное решение"
    buttonText="Связаться с нами"
  />

</Layout>

<style>
  /* Hero секция */
  .hero-section {
    padding: 5rem 2rem;
    background: white;
    position: relative;
    overflow: hidden;
  }

  .hero-section::before {
    content: '';
    position: absolute;
    top: 0;
    right: 0;
    width: 40%;
    height: 100%;
    background: linear-gradient(135deg, rgba(61, 17, 57, 0.03), rgba(229, 28, 39, 0.05));
    clip-path: polygon(30% 0, 100% 0, 100% 100%, 0% 100%);
  }

  .hero-container {
    max-width: 1100px;
    margin: 0 auto;
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: 2fr 1fr;
    gap: 3rem;
    align-items: center;
  }

  .hero-content {
    text-align: left;
  }

  h1 {
    font-family: var(--font-heading);
    font-weight: 300;
    font-size: 3.5rem;
    color: var(--color-primary);
    margin-bottom: 1rem;
    line-height: 1.2;
    background: none;
    -webkit-text-fill-color: var(--color-primary);
  }

  .slogan {
    font-family: var(--font-body);
    font-size: 1.125rem;
    color: var(--color-text);
    opacity: 0.8;
    text-transform: none;
    letter-spacing: normal;
  }

  .hero-decoration {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .decoration-circle {
    width: 200px;
    height: 200px;
    background: linear-gradient(135deg, var(--color-primary), var(--color-secondary));
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 20px 60px rgba(61, 17, 57, 0.2);
    position: relative;
  }

  .decoration-circle::before {
    content: '';
    position: absolute;
    width: 100%;
    height: 100%;
    border-radius: 50%;
    border: 3px solid var(--color-secondary);
    opacity: 0.3;
    animation: pulse 3s ease-in-out infinite;
  }

  @keyframes pulse {
    0%, 100% {
      transform: scale(1);
      opacity: 0.3;
    }
    50% {
      transform: scale(1.15);
      opacity: 0;
    }
  }

  .decoration-circle svg {
    width: 100px;
    height: 100px;
    color: white;
  }

  /* Секции */
  section {
    padding: 4rem 0;
  }

  /* Сетка проектов */
  .projects-section {
    background: linear-gradient(135deg, rgba(61, 17, 57, 0.02), rgba(229, 28, 39, 0.02));
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .container-narrow {
    max-width: 900px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .projects-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
    gap: 2rem;
    margin-top: 3rem;
  }

  /* Карточка проекта */
  .project-card {
    background: white;
    border: 1px solid rgba(61, 17, 57, 0.1);
    border-radius: 12px;
    box-shadow: 0 4px 20px rgba(61, 17, 57, 0.08);
    transition: all 0.3s ease;
    display: flex;
    flex-direction: column;
  }

  .project-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 30px rgba(61, 17, 57, 0.12);
    border-color: var(--color-secondary);
  }

  .project-card-header {
    padding: 2rem;
    border-bottom: 2px solid var(--color-secondary);
  }

  .project-card-header h2 {
    font-size: 2rem;
    margin-bottom: 0.5rem;
    background: none;
    -webkit-text-fill-color: var(--color-primary);
  }

  .project-description {
    color: var(--color-text);
    opacity: 0.75;
    font-size: 1.1rem;
    margin: 0;
  }

  .project-card-body {
    padding: 2rem;
    flex: 1;
  }

  .project-details {
    margin-bottom: 2rem;
    line-height: 1.6;
  }

  .project-features {
    margin-bottom: 2rem;
  }

  .project-features h4 {
    font-size: 0.9rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.03em;
    color: var(--color-primary);
    margin-bottom: 0.5rem;
  }

  .project-features ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .project-features li {
    padding: 0.25rem 0;
    color: var(--color-text);
    opacity: 0.8;
    font-size: 0.95rem;
  }

  .project-features li::before {
    content: "✓ ";
    color: var(--color-secondary);
    font-weight: bold;
    margin-right: 0.5rem;
  }

  .project-meta {
    background: rgba(61, 17, 57, 0.03);
    padding: 1rem;
    border-radius: 8px;
    margin-bottom: 2rem;
  }

  .meta-item {
    display: flex;
    justify-content: space-between;
    padding: 0.25rem 0;
    font-size: 0.9rem;
  }

  .meta-label {
    font-weight: 700;
    color: var(--color-primary);
  }

  .meta-value {
    color: var(--color-text);
    opacity: 0.8;
  }

  .status-badge {
    background: var(--gradient-brand);
    color: white;
    padding: 0.25rem 0.5rem;
    border-radius: 4px;
    font-size: 0.85rem;
    font-weight: 700;
  }

  .project-tags {
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;
  }

  .tag {
    display: inline-block;
    padding: 0.25rem 0.75rem;
    background: rgba(61, 17, 57, 0.08);
    color: var(--color-primary);
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    border-radius: 4px;
  }

  .project-card-footer {
    padding: 2rem;
    border-top: 1px solid rgba(61, 17, 57, 0.1);
  }

  .project-card-footer .btn {
    width: 100%;
  }

  /* Статистика */
  .stats-section {
    background: white;
  }

  .stats-section h2 {
    background: none;
    -webkit-text-fill-color: var(--color-primary);
  }

  .stats-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 2rem;
    margin-top: 3rem;
  }

  .stat-card {
    text-align: center;
    padding: 2rem;
  }

  .stat-number {
    font-family: var(--font-heading);
    font-weight: 200;
    font-size: 3.5rem;
    background: var(--gradient-brand);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 0.5rem;
  }

  .stat-label {
    font-family: var(--font-body);
    text-transform: uppercase;
    letter-spacing: 0.03em;
    color: var(--color-text);
    opacity: 0.7;
    font-size: 0.9rem;
  }

  /* Процесс работы */
  .process-section {
    background: linear-gradient(135deg, rgba(61, 17, 57, 0.02), rgba(229, 28, 39, 0.02));
  }

  .process-section h2 {
    background: none;
    -webkit-text-fill-color: var(--color-primary);
  }

  .process-steps {
    display: flex;
    flex-direction: column;
    gap: 2rem;
    margin-top: 3rem;
  }

  .process-step {
    display: flex;
    gap: 2rem;
    align-items: flex-start;
  }

  .step-number {
    font-family: var(--font-heading);
    font-weight: 200;
    font-size: 3rem;
    background: var(--gradient-brand);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    flex-shrink: 0;
    width: 80px;
  }

  .step-content h3 {
    margin-bottom: 0.5rem;
    background: none;
    -webkit-text-fill-color: var(--color-primary);
  }

  .step-content p {
    margin: 0;
    color: var(--color-text);
    opacity: 0.8;
  }

  /* CTA секция */
  .cta-section {
    background: var(--gradient-brand);
    color: white;
  }

  .cta-section h2 {
    color: white;
    background: none;
    -webkit-text-fill-color: white;
  }

  .slogan-cta {
    color: white;
    opacity: 0.95;
    text-transform: none;
    letter-spacing: normal;
  }

  .text-center {
    text-align: center;
  }

  .cta-buttons {
    display: flex;
    gap: 1rem;
    justify-content: center;
    margin-top: 2rem;
    flex-wrap: wrap;
  }

  .btn {
    display: inline-block;
    padding: 1rem 2rem;
    font-family: var(--font-body);
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.04em;
    border: none;
    border-radius: 8px;
    cursor: pointer;
    transition: all 0.3s ease;
    text-decoration: none;
    font-size: 0.95rem;
  }

  .btn-primary {
    background: white;
    color: var(--color-primary);
  }

  .btn-primary:hover {
    background: var(--color-bg);
    transform: scale(1.05);
  }

  .btn-secondary {
    background: transparent;
    color: white;
    border: 2px solid white;
  }

  .btn-secondary:hover {
    background: white;
    color: var(--color-primary);
  }

  /* Адаптивность */
  @media (max-width: 768px) {
    .hero-container {
      grid-template-columns: 1fr;
      text-align: center;
    }

    .hero-content {
      text-align: center;
    }

    h1 {
      font-size: 2.5rem;
    }

    .decoration-circle {
      width: 150px;
      height: 150px;
    }

    .decoration-circle svg {
      width: 75px;
      height: 75px;
    }

    .projects-grid {
      grid-template-columns: 1fr;
    }

    .stats-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .process-step {
      flex-direction: column;
      gap: 0.5rem;
    }

    .step-number {
      font-size: 2rem;
      width: auto;
    }

    .cta-buttons {
      flex-direction: column;
      align-items: center;
    }

    .cta-buttons .btn {
      width: 100%;
      max-width: 300px;
    }
  }
</style>