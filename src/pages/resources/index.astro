---
// src/pages/resources/index.astro
import Layout from '../../layouts/Layout.astro';

const base = import.meta.env.BASE_URL;
---

<Layout title="Ресурсы - StandartSoft Plus">
  <section class="hero-section">
    <div class="container">
      <h1>Ресурсы</h1>
      <p class="slogan">Полезные материалы и документация</p>
    </div>
  </section>

  <section class="resources-section">
    <div class="container">
      <a href={`${base}faq/`} class="resource-card">
        <div class="resource-icon">
          <svg viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
            <circle cx="12" cy="12" r="10" stroke="currentColor" stroke-width="2"/>
            <path d="M9.09 9a3 3 0 0 1 5.83 1c0 2-3 3-3 3" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            <circle cx="12" cy="17" r="1" fill="currentColor"/>
          </svg>
        </div>
        <h3>Вопросы и ответы</h3>
        <p>Часто задаваемые вопросы о наших услугах, процессах разработки и технологиях</p>
        <span class="resource-link">Перейти →</span>
      </a>

      <a href={`${base}documents/`} class="resource-card">
        <div class="resource-icon">
          <svg viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
            <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            <path d="M14 2v6h6M16 13H8M16 17H8M10 9H8" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </div>
        <h3>Документы</h3>
        <p>Нормативные документы, договоры, техническая документация и прайс-листы</p>
        <span class="resource-link">Перейти →</span>
      </a>
    </div>
  </section>
</Layout>

<style>
  .hero-section {
    padding: 4rem 2rem;
    text-align: center;
    background: linear-gradient(135deg, var(--color-primary), var(--color-accent-1));
    color: white;
  }

  h1 {
    font-family: var(--font-heading);
    font-weight: 200;
    font-size: 3rem;
    margin-bottom: 1rem;
  }

  .slogan {
    font-family: var(--font-body);
    font-size: 1.125rem;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    opacity: 0.95;
  }

  .resources-section {
    padding: 4rem 2rem;
  }

  .container {
    max-width: 1000px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
  }

  .resource-card {
    background: white;
    padding: 2.5rem;
    border-radius: 16px;
    box-shadow: 0 4px 20px rgba(61, 17, 57, 0.08);
    border: 1px solid rgba(61, 17, 57, 0.05);
    text-decoration: none;
    color: var(--color-text);
    transition: all 0.3s ease;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .resource-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 12px 40px rgba(61, 17, 57, 0.15);
    border-color: var(--color-secondary);
  }

  .resource-icon {
    width: 64px;
    height: 64px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, var(--color-primary), var(--color-accent-1));
    border-radius: 12px;
    color: white;
    margin-bottom: 0.5rem;
  }

  .resource-icon svg {
    width: 36px;
    height: 36px;
  }

  .resource-card h3 {
    font-family: var(--font-heading);
    font-weight: 300;
    color: var(--color-primary);
    font-size: 1.5rem;
    margin: 0;
  }

  .resource-card p {
    color: var(--color-text);
    opacity: 0.8;
    line-height: 1.6;
    margin: 0;
    flex: 1;
  }

  .resource-link {
    color: var(--color-secondary);
    font-weight: 600;
    font-size: 0.95rem;
    margin-top: 0.5rem;
    transition: all 0.2s ease;
  }

  .resource-card:hover .resource-link {
    transform: translateX(4px);
    display: inline-block;
  }

  @media (max-width: 768px) {
    h1 {
      font-size: 2rem;
    }

    .container {
      grid-template-columns: 1fr;
    }

    .resource-card {
      padding: 2rem;
    }
  }
</style>