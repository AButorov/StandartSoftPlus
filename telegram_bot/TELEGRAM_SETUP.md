# Настройка Telegram уведомлений

Подробная инструкция по настройке мгновенных уведомлений в Telegram при получении заявок с сайта.

## Архитектура

```
Сайт (форма) → Cloudflare Worker → Telegram Bot API → Уведомления
```

**Компоненты:**

1. **Telegram Bot** - создается через @BotFather
2. **Cloudflare Worker** - бессерверная функция для обработки запросов
3. **Форма контактов** - отправляет данные в Worker

**Преимущества:**

- ⚡ Мгновенная доставка (< 1 секунды)
- 🆓 Бесплатно (100,000 запросов/день)
- 📱 Уведомления на все устройства
- 👥 Поддержка нескольких получателей
- 🌍 Глобальная доступность через Cloudflare Edge Network

---

## Шаг 1: Создание Telegram бота

### 1.1. Открыть BotFather

1. Открыть Telegram
2. Найти в поиске: `@BotFather`
3. Открыть чат с официальным ботом (с синей галочкой)

### 1.2. Создать бота

Отправить команду:

```
/newbot
```

BotFather попросит ввести **имя бота**:

```
StandartSoft Plus Leads
```

Затем **username** (должен заканчиваться на `bot`):

```
standartsoftplus_leads_bot
```

### 1.3. Получить токен

BotFather вернёт сообщение с токеном:

```
Use this token to access the HTTP API:
7123456789:AAHdqTcvCH1vGWJxfSeofSAs0K5PALDsaw
```

**Сохранить токен** - он понадобится для Cloudflare Worker.

---

## Шаг 2: Получение Chat ID

Chat ID нужен чтобы бот знал куда отправлять уведомления.

### Способ 1: Через getUpdates API

#### Для себя:

1. Найти бота в Telegram: `@standartsoftplus_leads_bot`
2. Нажать **START** и написать: `Привет`
3. Открыть в браузере (заменить `<TOKEN>` на ваш токен):

```
https://api.telegram.org/bot<TOKEN>/getUpdates
```

Пример:

```
https://api.telegram.org/bot7123456789:AAHdqTcvCH1vGWJxfSeofSAs0K5PALDsaw/getUpdates
```

4. В JSON ответе найти:

```json
{
  "result": [{
    "message": {
      "chat": {
        "id": 987654321,  ← ВАШ CHAT_ID
        ...
      }
    }
  }]
}
```

**Сохранить:** `TELEGRAM_CHAT_ID_1=987654321`

#### Для партнера:

1. Отправить партнеру ссылку: `https://t.me/standartsoftplus_leads_bot`
2. Партнер нажимает START и пишет любое сообщение
3. Обновить страницу getUpdates
4. Найти новый `chat.id` - это Chat ID партнера

**Сохранить:** `TELEGRAM_CHAT_ID_2=555888999`

### Способ 2: Через @userinfobot (быстрее)

1. Найти в Telegram: `@userinfobot`
2. Нажать **START**
3. Бот сразу покажет:

```
Id: 987654321
First name: Александр
```

Chat ID = это значение `Id`.

---

## Шаг 3: Развертывание Cloudflare Worker

### 3.1. Войти в Cloudflare

1. Открыть https://dash.cloudflare.com
2. Войти в аккаунт
3. Перейти в **Workers & Pages**

### 3.2. Создать Worker

1. Нажать **"Create application"**
2. Выбрать **"Create Worker"**
3. Нажать **"Start with Hello World!"**

### 3.3. Настроить Worker

**Имя:**

```
telegram-notifier
```

**Код:**

Удалить весь шаблонный код и вставить содержимое файла `worker.js` из этой папки.

**Deploy:**

Нажать **"Save and Deploy"**

### 3.4. Скопировать URL Worker

После деплоя появится URL вида:

```
https://telegram-notifier.standartsoftplus.workers.dev
```

**Сохранить этот URL** - он понадобится для настройки проекта.

### 3.5. Добавить переменные окружения

1. Остаться на странице Worker или перейти: Workers & Pages → telegram-notifier
2. Перейти на вкладку **"Settings"**
3. Прокрутить до **"Variables and Secrets"**

Добавить 3 секрета:

#### Секрет 1: Bot Token

- Variable name: `TELEGRAM_BOT_TOKEN`
- Value: токен от BotFather
- Type: **Encrypt** (секрет)

#### Секрет 2: Chat ID 1

- Variable name: `TELEGRAM_CHAT_ID_1`
- Value: ваш Chat ID
- Type: **Encrypt**

#### Секрет 3: Chat ID 2

- Variable name: `TELEGRAM_CHAT_ID_2`
- Value: Chat ID партнера
- Type: **Encrypt**

4. Нажать **"Deploy"** для применения изменений

---

## Шаг 4: Настройка проекта

### 4.1. Локальная разработка

Добавить в `.env`:

```bash
PUBLIC_TELEGRAM_WORKER_URL=https://telegram-notifier.standartsoftplus.workers.dev
```

Перезапустить dev-сервер:

```bash
bun run dev
```

### 4.2. Production (GitHub)

1. Открыть: https://github.com/abutorov/StandartSoftPlus/settings/secrets/actions
2. Нажать **"New repository secret"**
3. Добавить:
   - Name: `PUBLIC_TELEGRAM_WORKER_URL`
   - Value: `https://telegram-notifier.standartsoftplus.workers.dev`
4. Сделать push в main для нового деплоя

---

## Шаг 5: Тестирование

### 5.1. Тест через curl

```bash
curl -X POST "https://telegram-notifier.standartsoftplus.workers.dev" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Тестовый клиент",
    "contactType": "Telegram",
    "contactValue": "@test_user",
    "message": "Проверка работы уведомлений"
  }'
```

**Ожидаемый результат:**

- В терминале: `{"success":true}`
- В Telegram (у вас и партнера): сообщение с данными заявки

### 5.2. Тест через сайт

**Локально:**

1. Открыть http://localhost:4321/contacts/
2. Заполнить форму
3. Отправить
4. Проверить Telegram

**Production:**

1. Открыть https://standartsoftplus.com/contacts/
2. Заполнить форму
3. Отправить
4. Проверить Telegram

---

## Мониторинг и отладка

### Просмотр логов Worker

1. Открыть https://dash.cloudflare.com
2. Workers & Pages → **telegram-notifier**
3. Вкладка **"Logs"**
4. Нажать **"Begin log stream"**
5. Отправить тестовую форму
6. Наблюдать логи в реальном времени

### Проверка переменных

В Cloudflare Dashboard:

1. Workers & Pages → telegram-notifier
2. Settings → Variables and Secrets
3. Убедиться что все 3 переменные установлены

### Тестирование Telegram Bot API

Прямой тест отправки сообщения:

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_TOKEN>/sendMessage" \
  -H "Content-Type: application/json" \
  -d '{
    "chat_id": <YOUR_CHAT_ID>,
    "text": "Тест прямой отправки"
  }'
```

---

## Возможные проблемы

### Проблема 1: Worker возвращает 500

**Причина:** Переменные окружения не установлены

**Решение:**

1. Проверить Variables and Secrets в Cloudflare
2. Убедиться что все 3 переменные добавлены
3. Проверить что переменные типа "Secret" (encrypted)
4. Сделать Deploy после изменений

### Проблема 2: Не приходят сообщения

**Причина:** Неправильный Chat ID или токен

**Решение:**

1. Проверить токен бота - должен быть от @BotFather
2. Проверить Chat ID - только цифры, без пробелов
3. Убедиться что вы написали боту (START)
4. Проверить логи Worker на ошибки

### Проблема 3: CORS ошибка в браузере

**Причина:** CORS headers не установлены или Worker не задеплоен

**Решение:**

1. Проверить что Worker код содержит CORS headers
2. Убедиться что Worker задеплоен (Save and Deploy)
3. Проверить URL Worker в .env

### Проблема 4: Сообщения приходят только одному получателю

**Причина:** Неправильный второй Chat ID

**Решение:**

1. Партнер должен написать боту START
2. Получить Chat ID партнера через getUpdates
3. Проверить что `TELEGRAM_CHAT_ID_2` установлен правильно

---

## Масштабирование

### Добавление получателей

Для добавления третьего получателя:

1. В Cloudflare Worker добавить переменную `TELEGRAM_CHAT_ID_3`
2. Обновить код `worker.js`:

```javascript
const chatIds = [
  env.TELEGRAM_CHAT_ID_1,
  env.TELEGRAM_CHAT_ID_2,
  env.TELEGRAM_CHAT_ID_3, // Новый получатель
];
```

3. Deploy Worker

### Изменение формата сообщения

Отредактировать переменную `message` в `worker.js`:

```javascript
const message = `
🔔 <b>Новая заявка</b>

Имя: ${data.name}
Контакт: ${data.contactValue}
Сообщение: ${data.message}
`;
```

Поддерживаемое форматирование (HTML):

- `<b>жирный</b>`
- `<i>курсив</i>`
- `<code>моноширинный</code>`
- Эмодзи: 🔔 📱 👤 💬 ⏰

---

## Стоимость и лимиты

**Cloudflare Workers Free Plan:**

- 100,000 запросов/день
- Unlimited CPU time (до 10ms на запрос)
- Глобальное распространение
- **Стоимость:** $0

**Telegram Bot API:**

- Бесплатно без лимитов
- 30 сообщений/секунду
- **Стоимость:** $0

**Итого:** Полностью бесплатное решение для малого и среднего бизнеса.

---

## Безопасность

✅ **Токен бота** хранится в Cloudflare Worker Secrets (encrypted)  
✅ **Chat ID** хранятся в Cloudflare Worker Secrets (encrypted)  
✅ **CORS** защищает от несанкционированных запросов  
✅ **Rate limiting** автоматически через Cloudflare  
✅ **HTTPS** обязателен для всех запросов

**Не делайте:**

- ❌ Не коммитьте токены в Git
- ❌ Не используйте HTTP (только HTTPS)
- ❌ Не делитесь токеном бота публично

---

## Полезные ссылки

- [Telegram Bot API Documentation](https://core.telegram.org/bots/api)
- [Cloudflare Workers Documentation](https://developers.cloudflare.com/workers)
- [BotFather Commands](https://core.telegram.org/bots#6-botfather)
- [Cloudflare Workers Pricing](https://workers.cloudflare.com/#pricing)

---

## Поддержка

При возникновении проблем:

1. Проверить логи Cloudflare Worker
2. Проверить консоль браузера (F12 → Network)
3. Протестировать Worker напрямую через curl
4. Проверить переменные окружения

Если проблема не решается - создать Issue в репозитории проекта.
